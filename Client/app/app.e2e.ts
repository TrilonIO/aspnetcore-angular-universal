describe('App', () => {

    beforeEach(() => {
        // browser.get('/');
    });

    it('e2e test should run', () => {
        let subject = true;
        let result = true;
        expect(subject).toEqual(result);
    });

});
